# Expo + Metro + pnpm monorepos resolve more reliably with a hoisted node_modules layout.
node-linker=hoisted
# Let dependencies see peers hoisted to the workspace root (React, React Native, etc.)
shamefully-hoist=true
